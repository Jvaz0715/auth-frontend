import React, { Component } from "react";

export class Login extends Component {
  render() {
    return <div>Login</div>;
  }
}

export default Login;


/*

Login.js is created as the app's login page where already existing users can log in to the app. In order to make more readable code, we do not directly put the Login.js code in the app.js file.

*/